@model WebApplication4.Models.AspNetRoles

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>AspNetRoles</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name) @*displaynamefor is an alias for the "field" in the model*@
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)@*Render a displaytemplate that matches the property type.*@
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        //The beginning of building a from form,
        @Html.AntiForgeryToken()

<div class="form-actions no-color">
    <input type="submit" value="Delete" class="btn btn-default" /> @*//Define the submit button. The submit button sends the form data to the server.*@
    @Html.ActionLink("Back to List", "Index")@*Display the text: Back to List of the connection with the controller index of the current page*@
</div>
    }
</div>
